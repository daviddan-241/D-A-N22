# Copy of .env for reference — actual .env is already in the repo
# ONLY .env is what matters — it's committed for Codespaces auto-load

GEMINI_API_KEY=AIzaSy...  # https://aistudio.google.com/apikey (free)
AIDER_MODEL=gemini/gemini-2.0-flash

DASHBOARD_PORT=3000
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=your-password
DASHBOARD_SECRET=random-secret-string
